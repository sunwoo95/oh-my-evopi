/**
 * Pre-rendered ASCII landing mark for evopi.
 *
 * An original half-block emblem: two ascending chevrons stacked into an upward
 * motion, evoking the evolve-and-iterate loop at the heart of the harness.
 * Single-width glyphs only (▄ █ ▀) so it aligns in any monospace terminal.
 */

/** ~10 rows, ≤32 cols. The default splash mark. Keep in sync with install.sh evopi_logo_line(). */
export const EVOPI_LOGO = `              ▄▄
            ▄█████▄
          ▄█████████▄
        ▄█████▀▀▀█████▄
      ▄█████▀     ▀█████▄
    ▄█████▀         ▀█████▄
   ▀▀▀▀       ▄▄       ▀▀▀▀
           ▄██████▄
         ▄██████████▄
       ▀▀▀        ▀▀▀`;
